name: Build PR35 APK with stable signature

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  documentation-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Stable-signed APK already published from product commit 49892576e81f6616dd22f58f3e862f795b69946a.'
